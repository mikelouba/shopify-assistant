<div class="marquee-strip">
  <div class="m-track" aria-hidden="true">
    {%- assign items = section.settings.items | split: ',' -%}
    {%- comment -%} Duplicate for seamless loop {%- endcomment -%}
    {%- for pass in (1..2) -%}
      {%- for item in items -%}
        <span class="m-item">{{ item | strip }} <span class="m-sep"></span></span>
      {%- endfor -%}
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Marquee Strip",
  "tag": "div",
  "settings": [
    {
      "type": "text",
      "id": "items",
      "label": "Items (comma-separated)",
      "default": "Gut Health,Sleep Support,Stress Relief,Immune Boost,Energy & Focus,Skin Glow,Hormonal Balance"
    }
  ],
  "presets": [{ "name": "Marquee Strip" }]
}
{% endschema %}
